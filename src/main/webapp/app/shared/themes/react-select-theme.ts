export const colors = {
  /*
   * multiValue(remove)/color:hover
   */
  danger: 'var(--danger)',

  /*
   * multiValue(remove)/backgroundColor(focused)
   * multiValue(remove)/backgroundColor:hover
   */
  dangerLight: 'var(--secondary)',

  /*
   * control/backgroundColor
   * menu/backgroundColor
   * option/color(selected)
   */
  neutral0: 'var(--dark)',

  /*
   * control/backgroundColor(disabled)
   */
  neutral5: 'var(--secondary)',

  /*
   * control/borderColor(disabled)
   * multiValue/backgroundColor
   * indicators(separator)/backgroundColor(disabled)
   */
  neutral10: 'var(--danger)',

  /*
   * control/borderColor
   * option/color(disabled)
   * indicators/color
   * indicators(separator)/backgroundColor
   * indicators(loading)/color
   */
  neutral20: 'var(--secondary)',

  /*
   * control/borderColor(focused)
   * control/borderColor:hover
   */
  neutral30: 'var(--info)',

  /*
   * menu(notice)/color
   * singleValue/color(disabled)
   * indicators/color:hover
   */
  neutral40: 'var(--info)',

  /*
   * placeholder/color
   */
  neutral50: 'var(--neutral-50)',

  /*  /!*
     * indicators/color(focused)
     * indicators(loading)/color(focused)
     *!/
    neutral60: 'var(--neutral-60)',

    neutral70: 'var(--neutral-70)',

    /*
     * input/color
     * multiValue(label)/color
      * singleValue/color
     * indicators/color(focused)
     * indicators/color:hover(focused)
     */
  neutral80: 'var(--info)',

  neutral90: 'var(--info)',

  /*
   * control/boxShadow(focused)
   * control/borderColor(focused)
   * control/borderColor:hover(focused)
   * option/backgroundColor(selected)
   * option/backgroundColor:active(selected)
   */
  primary: 'var(--info)',

  /*
   * option/backgroundColor(focused)
   */
  primary25: 'var(--success)',

  /*
   * option/backgroundColor:active
   */
  primary50: 'var(--success)',

  primary75: 'var(--success)',
};
